#include<stdio.h>
int g[10[10],x[10]
void GCL(int n)
{
	for(int i=1;i<=n;i++)
	{
		for(int j=1;j<=n;j++)
		{
			g[i][j]=0;
		}
	}
	for(int i=1;i<=e;i++)
	{
		scanf("%d%d",&u,&v);
		g[u][v]=g[v][u]=1;
	}
mcoloring(1);
}
int mcoloring(int k)
{
	do
	{
		nextvalue(k);
		if(x[k]==0)
		return;
		if(k==n)
		
